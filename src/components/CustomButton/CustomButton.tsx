import { _internal_ComponentButton, Button } from '@headlessui/react';

const CustomButton = ({ displayName }: _internal_ComponentButton) => {
	return <Button></Button>;
};

export default CustomButton;
